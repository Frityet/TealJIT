# 2026-06-29T20:49:00Z table types slice

Base commit: 31528940981a9cded5707ade809782f5a538c7c4

Subagents:
- Archimedes (019f1512-2578-7220-8fbc-6dde1ba86a39) performed a read-only pass over `tl/` table type semantics. Key reference points: arrays use `{T}`, maps use `{K:V}`, nested table types are valid, records remain distinct from homogeneous maps, and empty table inference is a core Teal behavior. No files edited by the subagent.

Implemented:
- Added parser-only Teal table type descriptors for arrays (`{T}`), maps (`{K:V}`), nested table types, and the `table` alias as `{any:any}`.
- Threaded table type ids through locals, globals, function parameters, returns, function signatures, casts, record fields, and table index expressions.
- Added table literal entry tracking with per-literal owners so nested literals like `{ a = {1, 2} }` keep outer and inner entries distinct.
- Added strict table index checks for key/value assignments and first-use inference for untyped empty tables (`local xs = {}; xs[1] = 10`).
- Preserved ordinary LuaJIT bytecode for table operations; this slice adds native parser/checker metadata only.

Verification:
- `make -C src clean && make -C src -j2`
- `cd test/tealjit && ./run.sh`
- `make clean && make amalg`
- `cd test/tealjit && ./run.sh`
- `make -C src clean && make -C src -j2`

Known follow-ups:
- Tuple table types (`{string, integer}`), generic table types, and full union-of-table semantics are still partial.
- Homogeneous record-to-map matching, array-to-map subtyping nuances, and table library typing are not complete.
- Literal diagnostics currently use native checker messages rather than Teal's exact wording.
- The metadata is still parser-local; cross-module exported type environments remain future work.

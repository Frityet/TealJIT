# 2026-06-29T20:26:39Z typed globals slice

Base commit: 3716addc857822cb61a7f1922d5fec50ec0465a4

Subagents:
- Gauss (019f14ff-d533-7e13-8160-05a8e9de6e36) inspected Teal global declaration semantics and recommended parser-only global metadata, root-owned record shapes, tests for typed globals/functions/records, and rejection of dotted `global function`.

Implemented:
- Added native parser metadata for Teal globals so `global x: T`, `global x: T = expr`, and `global x = expr` can be type checked and later looked up as typed globals.
- Added `global function name(...)` parsing for simple names, with function signature metadata reused by call checking.
- Added `global record Name ... end` body parsing and global record shape ownership so later field reads, field stores, and colon method calls continue to check against the declared record shape.
- Preserved Teal surface semantics by rejecting bare `type`/`record` declarations and dotted `global function tbl.name(...)`. Strict-mode-only differences remain in the strict checker paths.
- Added focused positive and negative TealJIT tests for global variables, inferred globals, global functions, global records, global type forward declarations, and bare declaration errors.

Verification:
- `make -C src -j2`
- `cd test/tealjit && ./run.sh`
- `make clean && make amalg`
- `cd test/tealjit && ./run.sh`
- `make -C src clean && make -C src -j2`
- `cd test/tealjit && ./run.sh`

Known follow-ups:
- Global metadata is still parser-local to the current chunk/function tree; cross-module exported type environments are not implemented yet.
- Record/type identity remains coarse and structural in this slice; nominal record identity, generics, arrays/maps, enums, interfaces, and complete Teal declaration semantics still need deeper implementation.
- This slice does not add typed bytecode/JIT specialization yet; it improves native parsing/checking metadata that later bytecode generation can consume.

# 2026-06-29T19:03:13Z post-scaffold checkpoint

- Commit: `43da391cfee0b97ed4cf0cac979dbf846ae32467`
- Branch: `wasm`
- Pushed: `origin/wasm`

## What landed

- Added explicit `wasm64` architecture metadata and target detection.
- Added a WASM target ABI header with synthetic local IDs for future IR lowering.
- Added host import wrappers for FFI/JIT integration.
- Added host-backed FFI library/symbol/call hooks for `LJ_TARGET_WASM`.
- Added a WASM mcode staging path that allocates byte buffers instead of native
  executable memory.
- Added `doc/wasm.md` with the host-assisted JIT/FFI contract and references.
- Added the initial subagent research/survey notes.

## Validation

- `make -C src -j$(nproc)` passed.
- Native FFI smoke test passed:
  `src/luajit -e 'local ffi=require("ffi"); ffi.cdef[[int abs(int);]]; assert(ffi.C.abs(-7)==7); collectgarbage(); print(jit.arch, jit.os, "ok")'`
- `git diff --check` passed before commit.
- `make -C src -n TARGET_FLAGS=-DLUAJIT_TARGET=LUAJIT_ARCH_WASM64` reaches
  the intended backend gate in `src/Makefile`.
- `make -C src -n CC='emcc -sMEMORY64=1'` reaches the same backend gate.

## Local toolchain probe

- `emcc --version`: Emscripten 6.0.1.
- `clang --target=wasm64-unknown-unknown -dM -E -x c /dev/null` defines
  `__wasm64__` and `__SIZEOF_POINTER__ 8`.
- `emcc -sMEMORY64=1 -dM -E -x c /dev/null` defines `__EMSCRIPTEN__`,
  `__wasm64__`, and `__SIZEOF_POINTER__ 8`.
- `rustc`/`cargo` are not installed in this container.

## Next

- Added host-side support code under `wasm/host/wasmtime/` for the raw
  `lj_wasm_import_*` imports called by `src/lj_wasm_host.c`.
- Host scaffold worker: `019f14c1-f72e-7db0-8227-c2fecc0ff43c`.
- Host scaffold validation: `make -C wasm/host/wasmtime check` passed.
- Start the VM backend decision: DynASM-to-Wasm versus a generated C/wasm VM
  path.

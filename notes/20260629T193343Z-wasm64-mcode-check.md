# 20260629T193343Z - wasm64 mcode/check coverage

Base commit: `9ca56b58`

## Slice

- Probed the remaining `wasm/tools` skipped sources with `emcc -sMEMORY64=1`.
- `luajit.c` and `lj_gdbjit.c` compile as wasm64 support objects without changes.
- `lj_mcode.c` only failed because the WASM mcode protection path leaves `mcode_protfail()` unused.
- `lj_asm.c` still fails at the missing target instruction emitter, and `ljamalg.c` follows that same failure through its amalgamated include.

## Changes

- Guarded `mcode_protfail()` out for `LJ_TARGET_WASM`; the WASM mcode scaffold uses host-compiled byte buffers and no native page protection transition.
- Narrowed `WASM64_SKIP_SRC` to the files blocked by the missing WASM assembler target: `lj_asm.c` and `ljamalg.c`.

## Next

- Add the first `lj_emit_wasm.h`/assembler surface so `lj_asm.c` can compile.
- Then replace the placeholder trace module with real IR-to-Wasm function generation.

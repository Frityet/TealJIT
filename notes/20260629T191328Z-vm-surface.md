# 2026-06-29T19:13:28Z wasm VM surface checkpoint

- Commit at start: `098c5acae4ea9d34d8adc662c5dab236b16d4d09`
- Branch: `wasm`

## What this slice adds

- `doc/wasm_vm.md` describes the WASM64 VM bring-up problem and the recommended
  interpreter-first path.
- `src/lj_vm_wasm.h` and `src/lj_vm_wasm.c` add a compiled manifest of the VM
  symbols the WASM backend must provide or route through C/host support.
- `src/lj_vm.h` now lets WASM use C/libm `floor`, `ceil`, and `trunc` helpers
  rather than requiring VM assembly for those math helpers.
- `doc/wasm.md` now points to the VM bring-up document.

## Validation

- `make -C src -j$(nproc)` passed with `lj_vm_wasm.o` included.

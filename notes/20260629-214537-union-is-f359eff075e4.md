# 20260629-214537 union `is` slice

Base commit: f359eff075e49af9f5e3d8df053e56e36b2509c0

Scope:
- Fixed `is` checks on union-typed source values so they are not folded as definitely false just because the source type is `union`.
- Added runtime lowering for union targets such as `x is string | integer`, emitted as an OR-chain of existing single-type `is` checks.
- Preserved existing runtime checks for primitive targets, including integer value checks.
- Added regression coverage for source-union checks, union-target checks, and concrete values checked against union targets.

Reference agent:
- Curie (019f1552-8c2c-7b00-9cdc-ab939f8aecc3) checked upstream Teal `is` semantics and confirmed `x: string | integer; x is string` is allowed, `x is string | integer` is allowed, and upstream rewrites union targets into an OR-chain.

Verification:
- `make -C src clean && make -C src -j2`
- `cd test/tealjit && ./run.sh`
- `make clean && make amalg`
- `cd test/tealjit && ./run.sh`
- `make -C src clean && make -C src -j2`
- `git diff --check`

Known follow-ups:
- This slice implements boolean/runtime semantics only. It does not yet implement Teal's flow narrowing inside `if x is T then ... else ... end`.
- Runtime checks for table or record union arms still use Lua type-level checks, not full structural/discriminator checks.

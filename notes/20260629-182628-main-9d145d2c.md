# 2026-06-29 18:26:28 UTC - main agent - 9d145d2c

Goal: add native Teal support to this LuaJIT fork, using `tl/` only as
reference material, with strict mode enabled by default and LuaJIT 2.1
bytecode compatibility preserved.

Current repo state:
- Branch: `v2.1`
- Commit: `9d145d2ca3db58493859c495489a0f08f627834f`
- `tl/` is present as an untracked reference checkout. Do not vendor or copy
  implementation code from it.

Initial implementation direction:
- Keep the existing bytecode reader/writer unchanged for v2.1 compatibility.
- Add a native C Teal front-end module and call it from the text load path.
- Detect Teal source primarily by `.tl` chunk names and later by explicit CLI
  controls.
- First milestone is a strict, native source front-end that buffers `.tl`
  input, performs initial type checks, erases Teal syntax to Lua source, and
  feeds the result into the existing LuaJIT parser/code generator.
- Later milestones can move from source erasure toward typed bytecode emission
  and JIT/static type hints without changing old bytecode dumps.

Subagents:
- Explorer `Euler`: Teal reference behavior note under `notes/`.
- Explorer `Kepler`: LuaJIT integration note under `notes/`.

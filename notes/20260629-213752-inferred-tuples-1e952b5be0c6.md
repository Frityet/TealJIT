# 20260629-213752 inferred tuple literal slice

Base commit: 1e952b5be0c60075b8725a0966f49a1b6de8c97b

Scope:
- Added native tuple inference for heterogeneous table literals with positive integer keys covering `1..n`.
- Preserved Teal's array-like behavior for homogeneous list literals such as `{1, 2}`.
- Supported explicit positive numeric-key tuple inference, including out-of-order keys like `{[2] = 7, [1] = "x"}`.
- Reused existing tuple slot metadata so inferred tuple literals get exact constant-index reads, slot assignment checks, and out-of-range checks.

Reference agent:
- Franklin (019f154c-6c72-7bb0-bf0c-c46b234f06db) checked upstream Teal literal inference semantics and confirmed heterogeneous list literals infer tuple tables, homogeneous list literals infer arrays, and explicit positive numeric keys participate in tuple inference.

Verification:
- `make -C src clean && make -C src -j2`
- `cd test/tealjit && ./run.sh`
- `make clean && make amalg`
- `cd test/tealjit && ./run.sh`
- `make -C src clean && make -C src -j2`
- `git diff --check`

Known follow-ups:
- Mixed record/list literals such as `{name = "Ada", "x", 1}` do not yet become records with tuple or array interfaces like upstream Teal.
- Numeric-key tuple inference currently requires exact positive integer coverage of `1..n`; this matches the intended fixed tuple slots but still needs broader literal-shape diagnostics.
- Diagnostics remain TealJIT-native rather than word-for-word upstream Teal.

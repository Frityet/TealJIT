# 20260629-215823 `is` false-branch narrowing slice

Base commit: a20609491aa92c228035a486731d94492365e6a7

Scope:
- Reworked local `is` narrowing to compute intersections instead of blindly assigning the target type.
- Added false-branch narrowing for `else` blocks by subtracting the tested type from the original local type.
- Preserved strict nil semantics while allowing `integer | nil` to narrow to `integer` in the true branch and `nil` in the false branch.
- Supported union targets by intersecting/subtracting matching arms, e.g. `string | integer` tested against `string | boolean`.
- Kept narrowing scoped to the branch and restored the original static local type after the branch.

Verification:
- `make -C src clean && make -C src -j2`
- `cd test/tealjit && ./run.sh`
- `make clean && make amalg`
- `cd test/tealjit && ./run.sh`
- `make -C src clean && make -C src -j2`
- `git diff --check`

Known follow-ups:
- `elseif` chains only use the most recent condition for the final `else` branch, so combined facts from earlier conditions are not accumulated yet.
- Compound boolean facts such as `not (x is T)`, `and`, and `or` are not propagated.
- Narrowing remains local-variable-only.

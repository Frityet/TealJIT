# 20260629-215210 `is` local narrowing slice

Base commit: 95a092552908d792efb6cc1bcb24aa56e4c0d3d6

Scope:
- Added parser metadata so boolean expressions produced by `local_var is T` remember the local register and target type.
- Applied that metadata while parsing the true branch of `if local_var is T then ... end`.
- Restored the original local static type before parsing `elseif`, `else`, or following statements.
- Covered primitive union narrowing (`string | integer` to `string` or `integer`) and strict nil narrowing (`integer | nil` to `integer`).

Reference agent:
- Curie (019f1552-8c2c-7b00-9cdc-ab939f8aecc3) confirmed upstream Teal records flow facts for `is` and narrows variables in the true branch.

Verification:
- `make -C src clean && make -C src -j2`
- `cd test/tealjit && ./run.sh`
- `make clean && make amalg`
- `cd test/tealjit && ./run.sh`
- `make -C src clean && make -C src -j2`
- `git diff --check`

Known follow-ups:
- False-branch narrowing is not implemented yet; `else` does not narrow to the remaining union members.
- Narrowing currently covers local variables only, not upvalues, globals, record fields, or indexed values.
- Compound boolean facts such as `x is string and ...` or `not (x is string)` are not propagated.

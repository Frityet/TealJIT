# 20260629-212041 tuple table type slice

Base commit: 44eaa333b129723b8d920c222a02f67197d70168

Scope:
- Added native parsing for annotated Teal tuple table types like `{string, integer}`.
- Tracked fixed tuple slot metadata separately from table literal entry metadata.
- Added exact constant positive-integer tuple indexing for reads and writes.
- Added tuple literal assignment checks for slot types, explicit numeric keys, shorter literals, and too-long literals.
- Preserved tuple slot metadata when table types are cloned across function scopes.
- Allowed non-constant integer indexing for homogeneous tuples, matching upstream Teal's collapsed element behavior where the current native descriptor can represent it.

Reference agents:
- Leibniz (019f1533-15c9-7862-9d18-7648b46c6a97) checked tuple syntax, tuple literal assignment, constant indexing, explicit numeric keys, and upstream reference files in `tl/`.
- Bohr (019f153d-ca40-7ca2-bf65-867dfb705671) checked upstream Teal variable tuple indexing semantics and confirmed homogeneous tuples should accept variable integer reads/writes.

Verification:
- `make -C src clean && make -C src -j2`
- `cd test/tealjit && ./run.sh`
- `make clean && make amalg`
- `cd test/tealjit && ./run.sh`
- `make -C src clean && make -C src -j2`
- `git diff --check`

Known follow-ups:
- Full Teal union support is still needed before heterogeneous tuple variable indexing can match upstream exactly, e.g. `{string, integer}` indexed by a variable should produce `string | integer`.
- Unannotated heterogeneous table literals are not yet inferred as tuple tables.
- Diagnostics are currently TealJIT-native and not always word-for-word upstream Teal diagnostics.

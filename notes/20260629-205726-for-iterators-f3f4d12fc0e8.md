# 2026-06-29T20:57:26Z typed for-iterators slice

Base commit: f3f4d12fc0e86516e1e3cc3718ee23707b754c29

Subagents:
- Ptolemy (019f1526-607b-77b3-936d-b9ab91995ffb) performed a read-only pass over `tl/` for generic-for semantics. Findings: Teal does not allow inline loop-variable annotations; `ipairs({T})` yields `integer, T`; `pairs({K:V})` yields `K, V`; `next` is typed through overloaded stdlib declarations rather than the same special-function path. No files edited by the subagent.

Implemented:
- Added parser-only iterator metadata to builtin `ipairs` and `pairs` calls.
- Stamped visible generic-for locals from builtin iterator metadata, so loop bodies see typed keys and values without changing LuaJIT bytecode.
- Added strict checks for too many loop variables on typed builtin iterators and for invalid `ipairs` use on non-array maps.
- Added focused positive/negative TealJIT tests for `ipairs`, `pairs`, nested iterator typing, inline annotation syntax rejection, and iterator arity.

Verification:
- `make -C src -j2`
- `cd test/tealjit && ./run.sh`
- `make -C src clean && make -C src -j2`
- `cd test/tealjit && ./run.sh`
- `make clean && make amalg`
- `cd test/tealjit && ./run.sh`
- `make -C src clean && make -C src -j2`

Known follow-ups:
- `next` iterator typing is still pending; Teal models it through overloaded stdlib declarations.
- Custom iterator return tuples are not yet propagated into loop locals.
- Warning-level behavior, such as warning for `pairs` on arrays, is not implemented yet.
